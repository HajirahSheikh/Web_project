@*@model myproject.Models.Book*@
@*@model IEnumerable<myproject.Models.GenreType>*@


<!--<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@Model.Title - Book Details</title>
    <style>
        .book-details {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }

            .book-details img {
                max-width: 300px;
                height: auto;
                margin-bottom: 20px;
            }

            .book-details div {
                margin-bottom: 10px;
            }
    </style>
</head>
<body>-->
    <!-- Form for searching books by genre -->
    <!--<form asp-controller="Library" asp-action="BookDetails" method="get">
        <input type="text" name="genre" placeholder="Enter Genre" />
        <button type="submit">Search</button>
    </form>-->

    <!-- Display the list of genres -->
    <!--<ul>
        @foreach (var genre in Model)
        {
            <li>@genre.Genre</li>
        }
    </ul>




    <div class="book-details">
        <img src="@Model.CoverImageUrl" alt="@Model.Title">
        <h1>@Model.Title</h1>
        <div><strong>Author:</strong> @Model.Author</div>
        <div><strong>Genre:</strong> @Model.Genre</div>
        <div><strong>Description:</strong> @Model.Description</div>
        <div><strong>ISBN:</strong> @Model.ISBN</div>
        <div><strong>Publisher:</strong> @Model.Publisher</div>
        <div><strong>Language:</strong> @Model.Language</div>
        <div><strong>Excerpts:</strong> @Model.BookExcerpts</div>
        <div><strong>About the Author:</strong> @Model.AboutAuthor</div>
    </div>
</body>
</html>-->
